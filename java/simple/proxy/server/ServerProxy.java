package simple.proxy.server;

import simple.proxy.Proxy;

public class ServerProxy implements Proxy {

    public void registerRenderers() {
        /* Server doesn't need to render graphics or entities! */
    }

}
